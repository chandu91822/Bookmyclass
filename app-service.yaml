apiVersion: v1
kind: Service
metadata:
  name: bookmyclass-app-service
  labels:
    app: bookmyclass-app
spec:
  type: NodePort
  selector:
    app: bookmyclass-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
